export default function Home() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-2">AI Agent Governance Platform</h1>
      <p className="text-slate-600">
        Use the nav above to view the agent registry, the pending approval
        queue, and the audit trail.
      </p>
    </div>
  );
}
